const trendingGifs = state => state.trendingGifs;
const randomGif = state => state.randomGif;
const searchGifs = state => state.searchGifs;
const activeRoute = state => state.activeRoute;

export default {
  activeRoute,
  trendingGifs,
  randomGif,
  searchGifs
};
